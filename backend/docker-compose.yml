services:
  mongo:
    image: mongo:latest
    container_name: tomato_mongo_instance
    restart: always
    mem_limit: 300m
    ports:
      - "27018:27018"
    volumes:
      - mongo_data:/data/db

  app:
    build:
      context: .
      dockerfile: dockerfile
      args:
        - PORT=${PORT}
    restart: always
    mem_limit: 300m
    container_name: CPEN321_tomato
    environment:
      - DB_URI=${DB_URI}
      - PORT=${PORT}
    ports:
      - "${PORT}:${PORT}"
    depends_on:
      - mongo
    command: npm start


volumes:
  mongo_data: